services:
  toolmux:
    environment:
      TOOLMUX_DISCOVERY_ROOTS: /host-home
    volumes:
      - type: bind
        source: ${TOOLMUX_HOST_HOME:?set TOOLMUX_HOST_HOME}
        target: /host-home
        read_only: true
